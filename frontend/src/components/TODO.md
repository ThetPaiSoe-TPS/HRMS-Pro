# TODO - Create empty HRMS-Pro components skeleton

- [x] Create component folder structure under `src/components/` as specified (common, layout, auth, employee, attendance, leave, payroll, department, position, report, settings, ui)
- [x] Create empty TSX files listed under each folder
- [x] Skip all `*.module.css` files (Tailwind will be used)
- [ ] Ensure existing `common/*/*.tsx` (AlertMessage/Button/Input/Modal) remain unchanged
- [ ] Verify TypeScript build/lint passes (if scripts exist)

